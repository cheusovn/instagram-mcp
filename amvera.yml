---
meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: true
  additionalCommands:
    - cd remotion-carousel && npm install --omit=dev --ignore-scripts
run:
  persistenceMount: /data
  command: node remotion-carousel/scheduler.mjs
